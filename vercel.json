{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm run typecheck:libs && node scripts/build-vercel.mjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null
}
